{
  "name": "tux_v1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tux_v1",
    "create-local": "graph create --node http://localhost:8020/ tux_v1",
    "remove-local": "graph remove --node http://localhost:8020/ tux_v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tux_v1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
